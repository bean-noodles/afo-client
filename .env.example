# Base URL of the AFO API backend. Leave unset to run on the bundled sample
# squad (no backend required).
VITE_API_BASE_URL=http://localhost:8000

# Optional: Google OAuth Client ID (Google Cloud Console) to enable the
# "Sign in with Google" button. Leave unset to disable it.
VITE_GOOGLE_CLIENT_ID=
